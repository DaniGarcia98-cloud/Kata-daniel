#
# Pages test websites

pages{
 Demo6 = "https://demo-autenticadordigital.labdigbdbstgsg.com/pg-main"
}

#
# Serenity parameters
#
serenity {
  browser.maximized = true
  console.colors = true
  logging = VERBOSE
  compromised.on = java.net.ConnectException
  fail.on = java.lang.IllegalAccessError
  timeout = 5000
}


webdriver {
  driver = Firefox
}

environment =    local2


environments {

  local {
    webdriver.driver = Chrome
    headless.mode =false
    chrome {
      switches = [
        "--start-maximized",
        "--ignore-certificate-errors",
        "--incognito",
        "--accept-insecure-certs",
        "--acceptSslCerts"
        "--disable-popup-blocking",
        "--disable-infobars",
        "--remote-allow-origins=*"
      ]
      preferences {
        download {
          prompt_for_download: false
          default_directory: "$TEMPDIR"
        }
      }
    }
  }
local2 {
   profile = "/Users/acastre/Library/Caches/Firefox/Profiles/ov4l4gox.default-release"
   webdriver.driver = Firefox
   headless.mode = false
   firefox {
      switches = [
         "--start-maximized",
         "--ignore-certificate-errors",
         "--incognito",
         "--accept-insecure-certs",
         "--acceptSslCerts",
         "--disable-popup-blocking",
         "--disable-infobars",
         "--remote-allow-origins=*"
      ]
      preferences {
         download {
            prompt_for_download: false
            default_directory: "$TEMPDIR"
         }
            intl.locale.requested: "es"
      }
   }
}

}